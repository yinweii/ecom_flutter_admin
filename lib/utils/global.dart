import 'package:flutter/cupertino.dart';
import 'package:intl/intl.dart';

class Global {
  static const avatar = 'images/avtdef.png';
  static String mesage =
      'a repository is the same as downloading, except it preserves the Git connection with the remote repository. You can then modify the files locally and upload the changes to the remote repository on GitLab';
}
